{
  "name": "@chessroyale/shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"    // Add clean script
  },
  "dependencies": {
    "zod": "^3.22.4",
    "drizzle-orm": "^0.29.3",
    "drizzle-zod": "^0.5.0",  // Add this
    "@neondatabase/serverless": "^0.7.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "drizzle-kit": "^0.20.14",
    "rimraf": "^5.0.0"       // Add this for clean script
  }
}
